.DS_Store

# Generated artifacts — rebuilt from EHBA-ruleset-rev230712-IT.md at build time.
# Never commit these (see CLAUDE.md golden rule): the next build overwrites them.
_site/
index.md
_body.md

# Jekyll / Bundler local state (never checked in; regenerated on each build).
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/
vendor/